import { GetterTree } from 'vuex';
import { IRootState } from '../../base/interfaces/IRootState';
import { IDynanicComponent } from '../interfaces/IDynanicComponent';

export const getters: GetterTree<IDynanicComponent, IRootState> = {};
